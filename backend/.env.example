# Copy to .env and fill in. App runs without these (mock script + browser voice),
# but real narration/audio need them.
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
# Optional: pick an ElevenLabs voice id (defaults to a standard voice)
ELEVENLABS_VOICE_ID=
